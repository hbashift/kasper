version: "3.7"

services:
  server:
    build:
      dockerfile: Dockerfile
    #image: "hbashift/kasper-back:v0.1.2"
    ports:
      - "8080:8080"
    restart: always
    volumes:
      - ./dissertations:/usr/src/app/dissertations
      - ./reports:/usr/src/app/reports
    extra_hosts:
      - "database:95.163.231.174"
    environment:
      MAIL_PASSWORD: "vVtVASLsViAdwF6"
#  front:
#    image: "hbashift/kasper-front:v0.1.5"
#    ports:
#      - "5173:5173"
#    restart: always
#    depends_on:
#      - server

networks:
  default:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1450
    name: kasper-test